<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Blog Website</title>
    <style>
        body {
            background-color: #f8f9fa;
        }
        .container {
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-top: 30px;
        }
        .hero {
            background-color: #007bff;
            color: #fff;
            padding: 60px 0;
            border-radius: 10px;
            margin-bottom: 40px;
            background-image: linear-gradient(to bottom right, #007bff, #0056b3);
        }
        .hero h1 {
            font-size: 3rem;
            font-weight: 700;
        }
        .hero p {
            max-width: 600px;
            margin: 0 auto;
            color: #fff;
            opacity: 0.8;
        }
        .card {
            transition: transform 0.2s;
            border: none;
        }
        .card:hover {
            transform: scale(1.02);
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }
        .btn-custom {
            background-color: #0056b3;
            color: #fff;
            border-radius: 50px;
        }
        .btn-custom:hover {
            background-color: #004080;
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="hero text-center">
            <h1 class="mb-4">Blog Website</h1>
            <p class="text-secondary fw-bold p-3 border rounded bg-light d-inline-block">Painting the World with Words</p>
            <a href="/articles/new" class="btn btn-custom btn-lg ms-2">Create Articles</a>
        </div>
        <% articles.forEach(article => { %>
            <div class="card mt-4 shadow-sm">
                <div class="card-body">
                    <h4 class="card-title"><%= article.title %></h4>
                    <div class="card-subtitle text-muted mb-2">
                        <%= article.createdAt.toLocaleDateString() %>
                    </div>
                    <div class="card-text mb-2"><%= article.description %></div>
                    <a href="articles/<%= article.slug %>" class="btn btn-primary">Read More</a>
                    <a href="articles/edit/<%= article._id %>" class="btn btn-info">Edit</a>
                    <form action="/articles/<%= article._id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </div>
            </div>
        <% }); %>
    </div>
</body>
</html>
